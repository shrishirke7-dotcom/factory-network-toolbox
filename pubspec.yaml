name: factory_network_toolbox
description: An advanced factory network utility and multi-VLAN MAC scanner tool.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # नेटवर्किंगसाठी लागणारे बेसिक पॅकेज
  network_info_plus: ^4.1.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
